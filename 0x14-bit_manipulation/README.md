Project ALx ***** 0x14. C - Bit manipulation *****

Documentations:

0. 0 unsigned int binary_to_uint(const char *b): function that converts a binary number to an unsigned int.

1. 1 void print_binary(unsigned long int n):function that prints the binary representation of a number.

2. 10 int get_bit(unsigned long int n, unsigned int index): function that returns the value of a bit at a given index.

3. 11 int set_bit(unsigned long int *n, unsigned int index):  function that sets the value of a bit to 1 at a given index.

4. 100 int clear_bit(unsigned long int *n, unsigned int index): function that sets the value of a bit to 0 at a given index.

5. 101 unsigned int flip_bits(unsigned long int n, unsigned long int m): function that returns the number of bits you would need to flip to get from one number to another.

6.  int get_endianness(void):function that checks the endianness.

7. Crackme3: Find the password for this program.
