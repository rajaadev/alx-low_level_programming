#include "main.h"
#include <stdio.h>
/**
* positive_or_negative - function  design number positif or negatif
* @i : integer
*/
void positive_or_negative(int i)
{
if ( i >= 0 )
printf("%d is positif\n",i);
else
printf("%d is negatiff\n",i);
}
