#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer.
 * @n: number to compute the absolute.
 * Return: int absuolute.
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
